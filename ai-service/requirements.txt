# ── CINESTATE AI Service Dependencies ────────────────────────
# No MongoDB. ClickHouse is the primary database.

# ── Web framework ─────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
python-jose[cryptography]>=3.3.0

# ── Google Cloud / Gemini / ADK ───────────────────────────────
# Hackathon-mandated package — includes ADK and Agent Engine
google-cloud-aiplatform[agent_engines,adk]>=1.101.0
# Gemini SDK for direct model calls (multimodal, structured output)
google-genai>=1.0.0
# Cloud Storage
google-cloud-storage>=2.16.0
# Secret Manager (production)
google-cloud-secret-manager>=2.20.0

# ── ClickHouse (Official Python Client + MCP Server) ─────────
clickhouse-connect>=0.7.19
mcp-clickhouse>=0.3.0

# ── Document / Media Processing ───────────────────────────────
pypdf>=4.3.1
python-docx>=1.1.2

# ── Data / Validation ─────────────────────────────────────────
pydantic>=2.7.0
pydantic-settings>=2.3.0

# ── Utilities ─────────────────────────────────────────────────
python-dotenv>=1.0.1
httpx>=0.27.0
aiofiles>=23.2.1
anyio>=4.4.0
structlog>=24.4.0
tenacity>=8.4.2

# ── Testing ───────────────────────────────────────────────────
pytest>=8.2.2
pytest-asyncio>=0.23.7
pytest-mock>=3.14.0
